<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/styles/stylesLogin.css">
</head>
<body>
    <div class="login-container">
        <img src="/img/tourify_logo.jpg" alt="Descripción de la imagen">
        <h1>Log in</h1>
        <form action="/login" method="post">
            <input type="text" id="usuario" name="usuario" placeholder="User" required>
            <input type="password" id="password" name="password" placeholder="Password" required>
            <button type="submit">Log in</button>
        </form>
        <% if (me) { %>
            <p class="error-message"><%= me %></p>
        <% } %>
        <div class="noLogin">
            No tienes contraseña? 
            <button onclick="window.location.href='/register'">Registrar</button>
        </div>
    </div>
</body>
</html>
